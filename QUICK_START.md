# Quick Start Guide

Этот документ поможет вам быстро развернуть обновленного бота с ComfyUI.

## ✅ Предварительные требования

- ✅ RunPod аккаунт с запущенным Pod (RTX A5000)
- ✅ ComfyUI установлен и работает
- ✅ Railway проект с PostgreSQL и Redis
- ✅ Telegram бот токен

## 🚀 Шаги развертывания

### 1. Установите модели на RunPod (15-30 минут)

Следуйте инструкциям в **[COMFYUI_MODEL_INSTALLATION.md](./COMFYUI_MODEL_INSTALLATION.md)**:

```bash
# SSH в RunPod
ssh root@your-runpod-ip

# Установите SDXL Refiner
cd /workspace/ComfyUI/models/checkpoints
wget https://huggingface.co/stabilityai/stable-diffusion-xl-refiner-1.0/resolve/main/sd_xl_refiner_1.0.safetensors

# Установите IP-Adapter
cd /workspace/ComfyUI/custom_nodes
git clone https://github.com/cubiq/ComfyUI_IPAdapter_plus.git
cd ComfyUI_IPAdapter_plus && pip install -r requirements.txt

# Установите LivePortrait
cd /workspace/ComfyUI/custom_nodes
git clone https://github.com/kijai/ComfyUI-LivePortraitKJ.git
cd ComfyUI-LivePortraitKJ && pip install -r requirements.txt

# Перезапустите ComfyUI
pkill -f "python.*main.py"
cd /workspace/ComfyUI
nohup python main.py --listen 0.0.0.0 --port 8188 > /workspace/comfyui.log 2>&1 &
```

### 2. Настройте автоматическую очистку файлов (5 минут)

```bash
# Скопируйте скрипт очистки
scp scripts/runpod_cleanup.sh root@your-runpod-ip:/workspace/cleanup.sh

# Сделайте его исполняемым
ssh root@your-runpod-ip "chmod +x /workspace/cleanup.sh"

# Добавьте в crontab
ssh root@your-runpod-ip "crontab -e"
# Добавьте эту строку:
0 * * * * /workspace/cleanup.sh >> /workspace/cleanup.log 2>&1
```

### 3. Обновите переменные окружения в Railway (2 минуты)

В Railway dashboard → Variables:

```bash
# Удалите старую переменную
REPLICATE_API_TOKEN  # ← удалить

# Добавьте новые переменные
COMFYUI_API_URL=https://o2zqe9gq92hyf2-8188.proxy.runpod.net
COMFYUI_API_PORT=8188
COMFYUI_POLL_INTERVAL=3
```

**Важно**: Замените URL на ваш актуальный URL из RunPod!

### 4. Разверните обновленный код (5 минут)

```bash
# Клонируйте или обновите репозиторий
cd /path/to/mybottg

# Добавьте все изменения
git add .

# Закоммитьте
git commit -m "Add production improvements: timeout, GPU limits, validation, cleanup"

# Отправьте на GitHub
git push origin main
```

Railway автоматически начнет развертывание.

### 5. Проверьте работу (5 минут)

1. **Откройте бота в Telegram**
2. **Отправьте `/start`**
3. **Нажмите "Генерация"**
4. **Отправьте промт**: `beautiful sunset over mountains`
5. **Дождитесь результата** (20-30 секунд)

Если все работает — поздравляем! 🎉

## 🔧 Настройка производительности

### Если генерация слишком медленная

Отредактируйте `workflows/sdxl_workflow_optimized.json`:

```json
{
  "3": {
    "inputs": {
      "steps": 15,  // Было 20, уменьшите до 15
      "cfg": 6.0    // Было 7.0, уменьшите до 6.0
    }
  }
}
```

### Если VRAM недостаточно

Отредактируйте `shared/redis_client_gpu.py`:

```python
MAX_GPU_JOBS = 1  # Оставьте 1 для безопасности
```

### Если очередь слишком длинная

Попробуйте увеличить до 2 параллельных задач (только для небольших изображений):

```python
MAX_GPU_JOBS = 2  # Осторожно! Может привести к OOM
```

## 📊 Мониторинг

### Проверьте логи Railway

```
Railway Dashboard → Service → Logs
```

Ищите:
- ✅ `ComfyUI generation completed`
- ✅ `Image generation successful`
- ⚠️ `GPU at capacity` (нормально при высокой нагрузке)
- ❌ `ComfyUI connection timeout` (проверьте RunPod)

### Проверьте использование диска на RunPod

```bash
ssh root@your-runpod-ip "df -h /workspace"
```

Если использование > 80%, запустите очистку вручную:

```bash
ssh root@your-runpod-ip "/workspace/cleanup.sh"
```

## ❓ Частые проблемы

### "Сервер генерации недоступен"

**Решение**: Проверьте, что RunPod Pod запущен и ComfyUI работает:

```bash
ssh root@your-runpod-ip "pgrep -f 'python.*main.py'"
```

Если процесс не найден, перезапустите ComfyUI:

```bash
cd /workspace/ComfyUI
nohup python main.py --listen 0.0.0.0 --port 8188 > /workspace/comfyui.log 2>&1 &
```

### "Генерация заняла слишком много времени"

**Решение**: Упростите промт или уменьшите количество шагов в workflow.

### "На фото не обнаружено лицо"

**Решение**: Убедитесь, что:
1. Фото содержит четкое изображение лица
2. Лицо хорошо освещено
3. Лицо не закрыто (очками, маской и т.д.)

### URL RunPod изменился после перезапуска

**Решение**: Обновите `COMFYUI_API_URL` в Railway:

1. Откройте RunPod dashboard
2. Скопируйте новый URL из "Connect" → "HTTP Service"
3. Обновите переменную в Railway
4. Перезапустите сервис в Railway

## 📚 Дополнительная документация

- **[PRODUCTION_IMPROVEMENTS.md](./PRODUCTION_IMPROVEMENTS.md)** — подробное описание всех улучшений
- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** — полное руководство по развертыванию
- **[MIGRATION_SUMMARY.md](./MIGRATION_SUMMARY.md)** — обзор изменений
- **[COMFYUI_MODEL_INSTALLATION.md](./COMFYUI_MODEL_INSTALLATION.md)** — установка моделей

## 🎯 Что дальше?

После успешного запуска рекомендуем:

1. **Мониторинг**: Следите за логами первые 24 часа
2. **Тестирование**: Проверьте все функции (изображения, видео, отмена)
3. **Оптимизация**: Настройте параметры под вашу нагрузку
4. **Резервное копирование**: Сделайте бэкап рабочей версии

Удачи! 🚀
